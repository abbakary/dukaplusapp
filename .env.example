# React app — API base (Vite dev server proxies /api to backend)
VITE_API_BASE_URL=/api/v1

# Backend URL for Vite proxy (optional override in vite.config.ts)
# VITE_BACKEND_URL=http://localhost:8000
